const assert = require('assert');
const nodePage = require('../index.js');

describe('nodePage', function () {
  it('has a test', function () {
    assert(false, 'nodePage should have a test');
  });
});
